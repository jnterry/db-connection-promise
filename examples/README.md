The subdirectories of this contain various examples for using the library.

Each directory contains a schema.sql which will setup the database appropriately for the scripts in the directory to be run.
