language: node_js

node_js:
  - stable

install:
  - npm install

script:
  - npm run cover

services:
  - mysql
  - postgres

env:
  - ANY_DB_Q_TEST_MYSQL=1 ANY_DB_Q_TEST_POSTGRES

# Send coverage data to Coveralls
after_script: "cat coverage/lcov.info | node_modules/coveralls/bin/coveralls.js"
